{
  imports = [
    ./alacritty.nix
    ./bat.nix
    ./git.nix
    ./gpg.nix
    ./kitty.nix
    ./lf.nix
    ./mbsync.nix
    ./neomutt.nix
    ./newsboat.nix
    ./niri.nix
    ./notmuch.nix
    ./password-store.nix
    ./qutebrowser.nix
    ./river.nix
    ./ssh.nix
    ./skim.nix
    ./starship.nix
    ./swaylock.nix
    ./tmux.nix
    ./zoxide.nix
    ./zsh.nix
  ];

  anoosa = {
    alacritty = {
      enable = true;
    };

    bat = {
      enable = true;
    };

    git = {
      enable = true;
    };

    gpg = {
      enable = true;
    };

    kitty = {
      enable = true;
    };

    lf = {
      enable = true;
    };

    mbsync = {
      enable = true;
    };

    neomutt = {
      enable = true;
    };

    newsboat = {
      enable = true;
    };

    niri = {
      enable = true;
    };

    notmuch = {
      enable = true;
    };

    password-store = {
      enable = true;
    };

    qutebrowser = {
      enable = true;
    };

    skim = {
      enable = true;
    };

    ssh = {
      enable = true;
    };

    tmux = {
      enable = true;
    };

    zsh = {
      enable = true;
    };
  };
}
