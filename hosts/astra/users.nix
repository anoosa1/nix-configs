{
  home-manager = {
    users = {
      anas = {
        anoosa = {
          git = {
            enable = true;

            user = {
              email = "anas@asherif.xyz";
            };
          };

          gpg = {
            enable = true;
          };

          lf = {
            enable = true;
          };

          mbsync = {
            enable = true;
          };

          neomutt = {
            enable = true;
          };

          newsboat = {
            enable = true;
          };

          notmuch = {
            enable = true;
          };

          password-store = {
            enable = true;
          };

          skim = {
            enable = true;
          };

          ssh = {
            enable = true;
          };

          tmux = {
            enable = true;
          };

          zsh = {
            enable = true;
          };
        };
      };
    };
  };
}
