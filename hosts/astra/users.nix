{
  home-manager = {
    users = {
      anas = {
        anoosa = {
          alacritty = {
            enable = true;
          };

          bat = {
            enable = true;
          };

          git = {
            enable = true;
          };

          gpg = {
            enable = true;
          };

          kitty = {
            enable = true;
          };

          lf = {
            enable = true;
          };

          mbsync = {
            enable = true;
          };

          neomutt = {
            enable = true;
          };

          newsboat = {
            enable = true;
          };

          niri = {
            enable = true;
          };

          notmuch = {
            enable = true;
          };

          password-store = {
            enable = true;
          };

          qutebrowser = {
            enable = true;
          };

          skim = {
            enable = true;
          };

          ssh = {
            enable = true;
          };

          tmux = {
            enable = true;
          };

          zsh = {
            enable = true;
          };
        };
      };
    };
  };
}
