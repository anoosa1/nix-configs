{
  anoosa = {
    soft-serve = {
      enable = true;
    };

    immich = {
      enable = true;
    };

    tailscale = {
      enable = true;
    };

    paperless = {
      enable = false;
    };
  };
}
